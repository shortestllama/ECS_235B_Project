[model]
names = ["Bell-LaPadula"]

[levels]
order = ["Public", "Secret"]

[categories]
sets = []

[objects]
"stdin" = { level = "Public", categories = [] }
"stdout" = { level = "Public", categories = [] }

[effects.scanf]
reads = "stdin"
writes_to_args = [1]

[effects.printf]
writes = "stdout"
reads_from_args = [1, 2]
